CREATE TABLE user_record(
                            user_id Long NOT NULL,
                            name VARCHAR(50) NOT NULL,
                            age int,
                            address VARCHAR(20) NOT NULL
);